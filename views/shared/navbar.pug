nav(class="navbar navbar-expand-lg navbar-light bg-light")
  a(class="navbar-brand" href='/' title="Er du jugepaven?!") Juge<strong>pave</strong>
  button(class="navbar-toggler" type='button' data-toggle='collapse'
         data-target='#navbarSupportedContent' aria-controls='navbarSupportedContent'
         aria-expanded='false' aria-label='Toggle navigation')
    span.navbar-toggler-icon
  
  div(id="navbarSupportedContent" class="collapse navbar-collapse" )
    ul.navbar-nav.mr-auto
      //- li.nav-item.active
      //-   a.nav-link(href='#') Hjem

      if rooms
        li.nav-item.dropdown
          a(id='navbarDropdownMenuLink' class="nav-link dropdown-toggle" 
            data-toggle='dropdown' aria-haspopup='true' aria-expanded='false') Rom
          .dropdown-menu(aria-labelledby='navbarDropdownMenuLink')
            each val, index in rooms
              a(href='/room/' + index class="dropdown-item")= val.name
            a(href='#' class="dropdown-item") test2
            a(href='#' class="dropdown-item") test3
      li.nav-item
        a.nav-link(href='/about') Spørsmål

    ul.navbar-nav.float-right.w-100
      if !user 
        li.nav-item
          a(href='/signup' class="nav-link text-right w-100") Lag bruker
        li.nav-item
          a(href='/' class="nav-link text-right w-100") Logg Inn
            

      if user
        li.nav-item.dropdown
          a(class="nav-link dropdown-toggle text-right w-100" data-toggle="dropdown" href="#"
            role="button" aria-haspopup="true" aria-expanded="false") Profil
          div(aria-labelledby='navbarDropdownMenuLink' class="dropdown-menu")
            a(href='/mypage' class="dropdown-item") Min profil
            a(href='#' class="dropdown-item") Innstillinger
            form(action="/logout" id="loggut" method="post")
              a(class="dropdown-item" href="javascript:{}" onclick="document.getElementById('loggut').submit(); return false;") Logg ut
